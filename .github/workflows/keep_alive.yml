name: Keep Streamlit App Alive (Ping Only)

on:
  schedule:
    - cron: '0 15,21,3,9 * * *'
  workflow_dispatch:

jobs:
  ping-app:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App URL
        run: |
          curl -I https://narouevalu-pgj8tq2tdlccjybdx88kvd.streamlit.app/ || true
          echo "Ping sent to app!"
